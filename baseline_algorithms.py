def bfs(graph, root):
    queue = []
    discovered = [False] * len(graph.data)

    discovered[root] = True
    queue.append()


def dfs(topology):
    best_route = []
    return best_route


def dijkstra(topology):
    best_route = []
    return best_route


def a_star(topology):
    best_route = []
    return best_route